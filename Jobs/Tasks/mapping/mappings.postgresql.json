{
  "Entities": {
    "Job": {
      "Fetch": {
        "ConnectionStringKey": "CommissionDB",
        "Function": "GetActiveJobs",
        "Script": "SELECT job_config_id, job_name, job_type, enabled, trigger_type, cron_expression, interval_seconds, start_at, end_at, parameters, created_at, updated_at FROM scheduler.job_config where enabled  = true"
      }
    }
  }
}